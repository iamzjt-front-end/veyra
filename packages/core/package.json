{
  "name": "@veyraoss/core",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@veyraoss/config": "workspace:*",
    "@veyraoss/protocol": "workspace:*",
    "@veyraoss/runtime": "workspace:*",
    "@veyraoss/verifier": "workspace:*",
    "@veyraoss/workflow": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests --maxWorkers=4",
    "clean": "rm -rf dist"
  },
  "description": "Provider-neutral orchestration and persisted run coordination.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iamzjt-front-end/veyra.git",
    "directory": "packages/core"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md",
    "CHANGELOG.md"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  }
}
