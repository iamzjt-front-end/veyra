{
  "name": "@veyraoss/project",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Veyra Project identity and project-owned state.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@veyraoss/protocol": "workspace:*",
    "yaml": "2.9.0"
  }
}
