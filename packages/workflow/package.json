{
  "name": "@veyra/workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./workflow-v1.schema.json": "./schema/workflow-v1.schema.json"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@veyra/protocol": "workspace:*",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
