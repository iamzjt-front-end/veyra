{
  "name": "@veyra/verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@veyra/protocol": "workspace:*",
    "@veyra/runtime": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "description": "Deterministic shell verification and command evidence.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iamzjt-front-end/veyra.git",
    "directory": "packages/verifier"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ]
}
