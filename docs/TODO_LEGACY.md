# Legacy TODO archive

This file is intentionally a pointer, not the canonical plan.

The pre-pivot detailed implementation plan is preserved in Git history at commit `b546e109a38a1613f55fc6cc34c653759effba5b` (`docs/TODO.md`).

Use the current [`docs/TODO.md`](TODO.md) for all new work. The product direction changed from API-first multi-provider orchestration to the project-centered, native-auth-first GPT ↔ Codex bridge described in [`PRODUCT.md`](../PRODUCT.md).

Do not resume tasks from the historical plan merely because they were previously blocked. API-provider smoke tests and additional-provider work are optional/deferred unless the current TODO explicitly reactivates them.
