# yaml-language-server: $schema=../../../packages/workflow/schema/workflow-v1.schema.json
name: minimal-command
version: 1
start: check
steps:
  check:
    type: command
    run:
      - node --version
    next: done
  done:
    type: end
