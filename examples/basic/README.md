# Basic example

The first working example will demonstrate:

```text
planner → executor → verifier → reviewer
                           ↑         |
                           └── fix ──┘
```

The example intentionally waits for the provider adapters and command runner to be implemented.
